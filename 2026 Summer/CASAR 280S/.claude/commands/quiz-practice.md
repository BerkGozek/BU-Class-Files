---
description: Generate practice short-essay quiz questions for a module in the course's known quiz format
argument-hint: [module number, e.g. "3"]
---

Generate quiz practice for Module $ARGUMENTS of CASAR 280S.

1. Read `Modules/Module $ARGUMENTS/Readings/` and its overview PDF (if present), plus the matching week's `Lecture Slides/` deck.
2. Following the format in quiz-format.md ("You are an archaeologist studying ___ in ___ region"), write 2-3 realistic practice short-essay questions covering this module's topics/regions.
3. For each, draft a model answer: complete sentences, no bullet points, fully explain the reasoning behind each part, answer every sub-part of the question, and use the module's actual key terms/methods rather than generic descriptions.
4. Present all questions first, then all model answers below a clear separator, so the user can self-test before checking answers.
