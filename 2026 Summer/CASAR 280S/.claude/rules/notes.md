# Notes

- PDFs require `poppler` to read inline: `brew install poppler`
- Completed assignments and quizzes go in the respective `Work Done/` subfolder (see [[assignments]] for the exact per-assignment layout, including folder-casing quirks) or `Quizes/`
- `Quizes/` is currently empty — no completed quizzes on file yet
